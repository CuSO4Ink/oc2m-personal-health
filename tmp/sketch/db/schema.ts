import { integer, sqliteTable, text, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const records = sqliteTable('records', { id: text('id').primaryKey(), tenant: text('tenant').notNull(), owner: text('owner').notNull(), title: text('title').notNull(), kind: text('kind').notNull(), date: text('date').notNull(), source: text('source').notNull(), summary: text('summary').notNull(), version: integer('version').notNull().default(1), created: text('created').notNull(), updated: text('updated').notNull(), fileKey: text('file_key'), fileName: text('file_name'), fileType: text('file_type'), fileSize: integer('file_size') }, t => [index('records_owner').on(t.tenant, t.owner)]);
export const versions = sqliteTable('versions', { id: text('id').primaryKey(), recordId: text('record_id').notNull().references(() => records.id), tenant: text('tenant').notNull(), version: integer('version').notNull(), title: text('title').notNull(), summary: text('summary').notNull(), actor: text('actor').notNull(), created: text('created').notNull() }, t => [uniqueIndex('versions_record_number').on(t.recordId, t.version)]);
export const grants = sqliteTable('grants', { id: text('id').primaryKey(), tenant: text('tenant').notNull(), owner: text('owner').notNull(), doctor: text('doctor').notNull(), recordId: text('record_id').notNull().references(() => records.id), expires: text('expires').notNull(), created: text('created').notNull(), revoked: text('revoked') }, t => [index('grants_access').on(t.tenant, t.doctor, t.recordId)]);
export const logs = sqliteTable('logs', { id: text('id').primaryKey(), tenant: text('tenant').notNull(), owner: text('owner').notNull(), actor: text('actor').notNull(), recordId: text('record_id'), title: text('title').notNull(), action: text('action').notNull(), result: text('result').notNull(), created: text('created').notNull() }, t => [index('logs_owner').on(t.tenant, t.owner, t.created)]);
export const sessions = sqliteTable('sessions', { id: text('id').primaryKey(), tenant: text('tenant').notNull(), actor: text('actor').notNull(), expires: text('expires').notNull() });
